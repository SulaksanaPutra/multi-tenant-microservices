# Archetype: A (API / Domain Service - Auth Plane)

## Overview
`user-service` acts as the Auth & User Identity layer in the system architecture.

## Architectural Bounds
- **Category:** Archetype A (API / Domain Service - Auth Plane)
- **Layout:** `cmd/` -> `internal/{handler, service, repository, domain}`
- **Responsibilities:** User authentication, identity management, JWT token management, user profile management.
