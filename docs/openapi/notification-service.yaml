openapi: "3.0.1"
info:
  title: Notification Service API
  description: "Auto-generated by `node scripts/generate-openapi.js` from the Gin routers and handler DTOs. Do not edit by hand."
  version: "1.0.0"
servers:
  -
    url: "http://localhost:8083"
tags:
  -
    name: notification-service
    description: Notification Service
paths:
  "/health":
    get:
      tags:
        - notification-service
      summary: Health check
      operationId: health
      description: Liveness probe returning HTTP 200 when the service is up.
      x-category: public
      x-service: notification-service
      x-source:
        - "notification-service/cmd/router.go:16"
      responses:
        "200":
          description: Successful response
          content:
            "application/json":
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                required:
                  - status
  "/api/notifications":
    get:
      tags:
        - notification-service
      summary: List Notifications
      operationId: ListNotifications
      security:
        -
          bearerAuth: []
      x-required-permission: "notifications:read"
      x-category: authenticated
      x-service: notification-service
      x-source:
        - "notification-service/cmd/router.go:30"
        - "notification-service/internal/handler/notification_handler.go:41"
      responses:
        "200":
          description: Successful response
          content:
            "application/json":
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      "$ref": "#/components/schemas/NotificationLogResponse"
                required:
                  - status
                  - data
        "400":
          description: "Bad Request — invalid request payload or validation failure"
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        "401":
          description: "Unauthorized — missing or invalid token"
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        "500":
          description: Internal Server Error
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    NotificationLogResponse:
      type: object
      properties:
        id:
          type: string
        user_id:
          type: string
        tenant_id:
          type: string
        description:
          type: string
        body:
          type: string
        status:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ErrorResponse:
      type: object
      required:
        - error
      properties:
        error:
          type: string
